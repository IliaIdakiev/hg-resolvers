import { AsyncRenderDirective } from './async-render.directive';

describe('AsyncRenderDirective', () => {
  it('should create an instance', () => {
    const directive = new AsyncRenderDirective();
    expect(directive).toBeTruthy();
  });
});
