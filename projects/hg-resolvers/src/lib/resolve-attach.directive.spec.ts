import { ResolveAttachDirective } from './resolve-attach.directive';

describe('ResolveAttachDirective', () => {
  it('should create an instance', () => {
    const directive = new ResolveAttachDirective();
    expect(directive).toBeTruthy();
  });
});
